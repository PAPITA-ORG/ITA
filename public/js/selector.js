$(document).ready(() => {
  // when user clicks...
  $("#btn-enviar").on("click", e => {});

  const submitUserInfo = () => {
    // console.log("new user!");
    // window.location.href = '/resultado'
  };
});
